#pragma once

/**
 * @file base/constants.h
 * @brief declaration graph constants.
 */

#include "types.h"

namespace graph_types
{

namespace constants {

static const uint32_t UP_LIMIT_OF_VERTEX_VALUE = (1 << 24) - 1;
static const vertex VERTEX_0 = vertex(0);

}

}
